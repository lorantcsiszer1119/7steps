<% if user_signed_in? %>
	<li>
		<%= current_or_guest_user.email %>
	</li>
	<li>
		<%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "small whitebutton right") %>        
	</li>
<% else %>
  	<li>
  		<%= link_to('Login', new_user_session_path, class: "small whitebutton right")  %>  

  	</li>
<% end %>